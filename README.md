# CI/CD Pipeline with GitHub Actions + Render

This project automatically deploys a Dockerized Node.js app to Render whenever code is pushed to the `main` branch.

## Technologies
- GitHub Actions
- Docker
- Render (free tier)
- Node.js + Express

## Live Demo
(Add your Render URL here after deployment)
